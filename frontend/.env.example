# Frontend Environment Variables (Vite)
# Leave blank for local development - the vite.config.js will proxy to http://localhost:5000/api
# For production deployment, set this to your deployed API URL

# VITE_API_URL=https://your-deployed-domain.vercel.app/api
